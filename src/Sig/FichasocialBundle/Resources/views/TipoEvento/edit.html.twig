{% extends '::base.html.twig' %}

{% block title %}TipoEvento{% endblock %}

{% block body -%}
    <ol class="breadcrumb">
        <li><a href="{{ path('root') }}">Inicio</a></li>
        <li><a href="{{ path('tiposevento') }}">TipoEvento</a></li>
        <li><a href="{{ path('tiposevento_show', { 'id': entity.id }) }}">Ver TipoEvento</a></li>
        <li class="active">Editar TipoEvento</li>
    </ol>
    <div class="panel panel-default">
        <div class="panel-heading nopadding">
            <h1>Editar TipoEvento ID: {{ entity.id }}</h1>
        </div>
        <div class="panel-body">
        {{ form(edit_form) }}
        </div>
    </div>
{% endblock %}
