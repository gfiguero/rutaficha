{% extends '::base.html.twig' %}

{% block title %}Domicilio{% endblock %}

{% block body -%}
    <ol class="breadcrumb">
        <li><a href="{{ path('root') }}">Inicio</a></li>
        <li><a href="{{ path('domicilios') }}">Domicilio</a></li>
        <li><a href="{{ path('domicilios_show', { 'id': entity.id }) }}">Ver Domicilio</a></li>
        <li class="active">Editar Domicilio</li>
    </ol>
    <div class="panel panel-default">
        <div class="panel-heading nopadding">
            <h1>Editar Domicilio ID: {{ entity.id }}</h1>
        </div>
        <div class="panel-body">
        {{ form(edit_form) }}
        </div>
    </div>
{% endblock %}
